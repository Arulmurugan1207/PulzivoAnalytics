{
  "name": "pulzivo-analytics-api",
  "version": "1.0.0",
  "private": true,
  "description": "Distinct Cloud Run service for Pulzivo / Table Tennis Tube analytics ingest. Does not replace the Pulzivo marketing SPA Cloud Run service named analytics.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "node --test server.spec.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.2",
    "express-rate-limit": "^7.5.1",
    "helmet": "^8.1.0",
    "mongodb": "^6.16.0"
  }
}
